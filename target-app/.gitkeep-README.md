# target-app

The deliberately hostile local Express app (SPEC §10, P0). Built first — everything
downstream depends on being able to seed failures on demand.

Tenant variant B (second port) is [T1] — see SPEC §15.
