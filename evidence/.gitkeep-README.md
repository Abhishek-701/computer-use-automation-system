# evidence

Committed run output (SPEC §13). Populated in CP7/CP8:

- `capability.json` — the saved artifact
- `discovery/` — structured log of the real LLM run
- `replay-success/` — clean replay with inputs and outputs
- `replay-outcomes/` — one subdirectory per demonstrated seeded condition
- `escalation/` — intervention request payload, before/after state, resume log
- `README.md` — index explaining what each directory demonstrates

This file is a scaffold placeholder only — no real evidence exists yet.
