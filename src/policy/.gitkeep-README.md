# src/policy

`enforce()` — the single door to the surface adapter (SPEC invariant #1, §8).

- `gate.ts` — enforce(); allow / block / require_approval
- `policy.default.json` — allowlist, action rules, redaction patterns
- `redact.ts` — redaction logic, called only from the log sink and artifact writer

Built in CP4 (SPEC P3).
