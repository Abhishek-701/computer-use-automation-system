# src/cli

`index.ts` — discover | replay | show | catalog | stability

`discover` requires `--goal` and `--target` (entry_point URL); target becomes
`artifact.target.entry_point`. See SPEC §17 for exact demo commands.
