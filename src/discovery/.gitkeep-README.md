# src/discovery

Tool-calling loop: observe -> decide -> act (SPEC P5).

- `loop.ts` — the loop, stopping conditions
- `prompt.ts` — system prompt / tool definitions for the LLM
- `recorder.ts` — trajectory -> artifact, pruning, parameterisation

Built in CP6 (SPEC P5). Verification replay against src/replay is mandatory before persist.
