# src/surface

ONLY place that knows about Playwright (SPEC.md invariant #2, §4).

- `adapter.ts` — SurfaceAdapter interface (perception + action)
- `web.playwright.ts` — the one real implementation
- `desktop.stub.ts` — interface-satisfying stub, NotImplemented bodies
- `observation.ts` — AX tree normalisation and pruning

Built in CP3 (SPEC P2).
