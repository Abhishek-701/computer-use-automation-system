# src/schema

zod schemas, single source of truth; types inferred, never hand-duplicated (SPEC §3).

- `artifact.ts` — capability artifact schema, semver, inferred types
- `result.ts` — replay result contract
- `action.ts` — closed Action union, shared by discovery and replay

Built in CP2 (SPEC P1) — highest-weighted artefact in the rubric.
