# src/evidence

Single log sink; redaction happens here, not at call sites (SPEC invariant #3, §8).

- `logger.ts` — single sink, redacts here
- `capture.ts` — screenshot / trace / observation snapshot

Built in CP7 (SPEC P6). Not to be confused with the top-level /evidence/ output bundle.
