# src/replay

Deterministic executor, no model in the loop (SPEC P4). Built *before* discovery.

- `engine.ts` — deterministic executor
- `locator.ts` — candidate resolution + drift reporting
- `assert.ts` — expectations and checkpoints
- `detect.ts` — generic matcher for declared outcome detectors

Built in CP5 (SPEC P4).
