# src/escalation

`control_owner` state machine + human takeover on the *same* live session (SPEC §9).

- `session.ts` — control_owner state machine
- `server.ts` — intervention endpoint + mock operator page

Built in CP7 (SPEC P6).
